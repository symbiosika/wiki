# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# public static files
public/manage/**/*aes-keys.generated.env

# Built SPA bundle, copied into ./static/app by the frontend build (and into the
# image by CI). Never part of the repository — it used to be easy to sweep into a
# commit with `git add -A` after a local build.
static/app/
